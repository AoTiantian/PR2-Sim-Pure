# pr2_wrench_input

Validation wrench input package for the PR2 whole-body admittance launch.

The current node publishes the virtual external wrench and validation timing
signals used by `pr2_qp_whole_body_admittance.launch.py`.
